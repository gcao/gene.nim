(HTML
  (HEAD
    (TITLE "Test HTML")
  ) # HEAD
  (BODY
    (HEADER ^style {^font-size 22}
      "This is the header section"
    )
    (DIV ^style {^font-size 10}
      "First section"
    )
  ) # BODY
) # HTML

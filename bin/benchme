#!/usr/bin/env bash

# Usage:
# benchme|copy

date "+%Y-%m-%d %H:%M:%S %A"
echo Git commit: $(git rev-parse HEAD)

BUILD_CMD="nimble build --gc:orc -d:release -d:ssl --out:bin/gene"
EXEC_CMD="examples/fib24.gene"
echo $BUILD_CMD
$BUILD_CMD && (echo $EXEC_CMD; $EXEC_CMD)

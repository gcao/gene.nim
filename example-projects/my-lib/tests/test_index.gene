#!/usr/bin/env gene

(import genex/tests/[test skip_test])

(import str_size from "src/index")

(test "Package"
  (assert (($pkg .name) == "my-lib"))
)

(test "str_size"
  (assert ((str_size "ab") == 2))
)
